# Test environment — committed intentionally (no secrets).
# Vitest reads this file and exposes VITE_* vars as import.meta.env.VITE_*
# so that tests run against the local dev backend URL without any fallback hacks.
VITE_API_URL=http://localhost:3000
